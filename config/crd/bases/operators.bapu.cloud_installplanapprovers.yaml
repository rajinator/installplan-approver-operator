---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: installplanapprovers.operators.bapu.cloud
spec:
  group: operators.bapu.cloud
  names:
    kind: InstallPlanApprover
    listKind: InstallPlanApproverList
    plural: installplanapprovers
    shortNames:
    - ipa
    singular: installplanapprover
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InstallPlanApprover is the Schema for the installplanapprovers
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InstallPlanApproverSpec defines the desired state of InstallPlanApprover.
            properties:
              autoApprove:
                default: true
                description: AutoApprove enables automatic approval of InstallPlans
                type: boolean
              operatorNames:
                description: |-
                  OperatorNames is an optional list of operator names to approve
                  If empty, approves all operators
                items:
                  type: string
                type: array
              targetNamespaces:
                description: |-
                  TargetNamespaces is a list of namespaces to watch for InstallPlans
                  If empty, watches all namespaces
                items:
                  type: string
                type: array
            type: object
          status:
            description: InstallPlanApproverStatus defines the observed state of InstallPlanApprover.
            properties:
              approvedCount:
                description: ApprovedCount is the total number of InstallPlans approved
                format: int32
                type: integer
              lastApprovedPlan:
                description: LastApprovedPlan contains the name of the last approved
                  InstallPlan
                type: string
              lastApprovedTime:
                description: LastApprovedTime is the timestamp of the last approval
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
